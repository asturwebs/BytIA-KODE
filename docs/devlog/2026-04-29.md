# 2026-04-29 — Skills System v2.0: Vendor + User + BytIA Layers

## Contexto

BytIA-KODE era independiente pero ahora integra con el ecosistema BytIA. Surgió la necesidad de:

1. **Usuario nuevo**: instala KODE, tiene skills funcionando sin ~/bytia/
2. **Usuario con BytIA**: instala KODE, puede compartir skills con otros assistants
3. **Dual-mode**: KODE funciona standalone o integrado

## Decisión: Arquitectura de Capas

```
~/.bytia-kode/skills/
├── bytia/      # ~/bytia/skills/ (si existe) — prioridad más alta
├── user/       # Skills propias del usuario — writable
└── vendor/     # Skills core incluidas con KODE — read-only
```

Prioridad: **bytia > user > vendor**

## Implementación

### SkillLoader Reescrito

```python
class SkillLoader:
    def get_layer_dirs(self) -> list[tuple[Path, str]]:
        layers = []
        
        # 1. bytia (si existe ~/bytia/)
        bytia_dir = self.bytia_home / "skills"
        if bytia_dir.exists():
            layers.append((bytia_dir, "bytia"))
        
        # 2. user (siempre writable)
        user_dir = self.skills_home / "user"
        if user_dir.exists():
            layers.append((user_dir, "user"))
        
        # 3. vendor (siempre, bundled)
        vendor_dir = self.skills_home / "vendor"
        if vendor_dir.exists():
            layers.append((vendor_dir, "vendor"))
        
        return layers
```

### Vendor Skills

Creado `src/bytia_kode/vendor/skills/` con:
- `bytia-constitution/` — Identidad
- `bytia-memory/` — Gestión de memoria
- `skills-manager/` — Gestión del sistema
- `graphify/` — Knowledge graphs

### install.sh Actualizado

```bash
# Estructura vendor/user
mkdir -p ~/.bytia-kode/skills/{vendor,user}
cp -r vendor_skills/* ~/.bytia-kode/skills/vendor/

# Integración opcional con BytIA
if [ -d "$HOME/bytia/skills" ]; then
    ln -s "$HOME/bytia/skills" ~/.bytia-kode/skills/bytia
fi
```

## Tests Añadidos

- `test_skill_loader_layer_priority` — User override vendor
- `test_skill_loader_bytia_layer_highest_priority` — BytIA override todo
- `test_skill_loader_all_layers_loaded` — Todos los layers cargan

**Total: 133 tests passing**

## Archivos Modificados

| Archivo | Cambio |
|---------|--------|
| `src/bytia_kode/skills/loader.py` | Reescrito para capas |
| `src/bytia_kode/config.py` | Auto-instalación vendor skills |
| `install.sh` | Integración BytIA opcional |
| `README.md` | Nueva sección Skills System |
| `pyproject.toml` | Versión → 0.7.5 |
| `CHANGELOG.md` | Entrada v0.7.5 |
| `src/tests/test_basics.py` | 3 tests nuevos |

## Comandos Útiles

```bash
# Instalar KODE con integración BytIA
curl -fsSL https://raw.githubusercontent.com/asturwebs/BytIA-KODE/main/install.sh | bash

# Ver skills cargadas
uv run python -c "from bytia_kode.skills.loader import SkillLoader; print(SkillLoader().load_all().keys())"

# Skills info
uv run python -c "from bytia_kode.skills.loader import SkillLoader; print(SkillLoader().get_skill_info())"
```

## Notas

- Vendor skills son **read-only** — verificación copia a user layer
- Usuario con ~/bytia/ puede linking durante install o después
- KODE funciona **standalone** sin ~/bytia/
